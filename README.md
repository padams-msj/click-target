# click-target
- Improve the styling.
- Change the frequency of the target moving from being hardcoded to 800ms to being a variable.
- Once it's a variable, add some way for the user to change the difficulty by changing how often the target moves. For example, you could add a dropdown menu with options like "Easy", "Medium", and "Hard" that correspond to different movement frequencies (e.g., Easy = 1200ms, Medium = 800ms, Hard = 500ms).
- Make clicks worth more points on higher difficulty levels. For example, Easy could be worth 1 point per click, Medium could be worth 2 points, and Hard - could be worth 3 points.
- Prevent the player from clicking the "Start Game" button once a game is underway - currently this causes problems.
